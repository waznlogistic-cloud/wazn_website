# Supabase Quick Setup Guide

دليل سريع لإعداد Supabase بعد إنشاء الجداول

---

## ✅ ما تم إنجازه:
- [x] إنشاء الجداول (Tables)

---

## ⚠️ ما يحتاج إعداده الآن:

### 1. تعطيل Email Confirmation (مهم جداً!)

**لماذا؟** 
- بدون تعطيله، المستخدمون لن يحصلوا على session مباشرة بعد التسجيل
- سيحتاجون للتحقق من البريد الإلكتروني أولاً

**كيف؟**
1. اذهب إلى Supabase Dashboard
2. Authentication → Settings
3. ابحث عن **"Enable email confirmations"**
4. **عطله** (Turn OFF)
5. احفظ التغييرات

**النتيجة:** بعد التسجيل، سيحصل المستخدم على session مباشرة ويمكنه الدخول فوراً.

---

### 2. Row Level Security (RLS) - اختياري للآن

**ملاحظة:** الجداول حالياً بدون RLS policies، مما يعني:
- ✅ يمكن للمستخدمين الوصول إلى بياناتهم
- ⚠️ لكن قد تحتاج RLS لاحقاً للأمان

**للآن:** يمكنك المتابعة بدون RLS للاختبار المحلي.

---

### 3. Storage Buckets (للملفات) - اختياري للآن

**متى تحتاجه؟**
- رفع المستندات (رخص، وثائق، إلخ)
- رفع صور

**للآن:** يمكنك المتابعة بدون Storage للاختبار.

---

## 🚀 الخطوات السريعة:

### الخطوة 1: تعطيل Email Confirmation (ضروري!)

```
1. افتح Supabase Dashboard
2. Authentication → Settings
3. ابحث عن "Enable email confirmations"
4. عطله
5. Save
```

### الخطوة 2: اختبر التسجيل

```
1. سجل كـ Employer
2. يجب أن يتم توجيهك مباشرة إلى /employer/profile
3. إذا تم التوجيه → كل شيء يعمل! ✅
4. إذا لم يتم التوجيه → تحقق من Email Confirmation
```

---

## 🔍 التحقق من الإعدادات:

### في Supabase Dashboard:

1. **Authentication → Settings**
   - [ ] Email confirmations: **Disabled**
   - [ ] Site URL: صحيح
   - [ ] Redirect URLs: صحيحة

2. **Database → Tables**
   - [ ] جميع الجداول موجودة:
     - profiles ✅
     - providers ✅
     - orders ✅
     - provider_drivers ✅
     - transactions ✅
     - proof_of_delivery ✅
     - permits ✅
     - notifications ✅

3. **Database → Tables → profiles**
   - [ ] افتح Table Editor
   - [ ] تأكد من وجود البيانات بعد التسجيل

---

## ⚠️ مشاكل شائعة:

### المشكلة 1: لا يتم التوجيه بعد التسجيل
**الحل:** 
- تأكد من تعطيل Email Confirmation
- تحقق من Console للأخطاء
- تأكد من أن `.env` يحتوي على Supabase credentials الصحيحة

### المشكلة 2: خطأ في إنشاء Profile
**الحل:**
- تحقق من أن جدول `profiles` موجود
- تحقق من أن `id` في profiles يطابق `id` في auth.users
- تحقق من Console للأخطاء

### المشكلة 3: لا يمكن الوصول للبيانات
**الحل:**
- تحقق من RLS policies (قد تحتاج تعطيلها للاختبار)
- تحقق من أن المستخدم مسجل دخول
- تحقق من Console للأخطاء

---

## 📝 Checklist سريع:

- [ ] Email Confirmation معطل
- [ ] جميع الجداول موجودة
- [ ] `.env` يحتوي على Supabase credentials
- [ ] جرب التسجيل كـ Employer
- [ ] تحقق من التوجيه إلى `/employer/profile`
- [ ] تحقق من البيانات في Supabase Dashboard

---

## 🎯 الخطوة التالية:

بعد تعطيل Email Confirmation:
1. جرب التسجيل كـ Employer
2. يجب أن يتم توجيهك مباشرة إلى `/employer/profile`
3. إذا عمل → جاهز للاختبار! ✅

---

## 💡 ملاحظات:

- **للاختبار المحلي:** يمكنك المتابعة بدون RLS
- **للإنتاج:** ستحتاج RLS policies للأمان
- **Storage:** يمكنك إضافته لاحقاً عند الحاجة

---

**الآن: اذهب إلى Supabase Dashboard وعطل Email Confirmation!** 🚀

